# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Streamlit local secrets (never commit credentials)
.streamlit/secrets.toml

# Notebook checkpoints
.ipynb_checkpoints/

# Scratch
*.log
*.tmp
~$*

# Bird audio for the ear test. The mp3s themselves are third-party
# Creative Commons material, so whether to commit them is a licensing decision
# rather than a technical one: CC BY and CC BY-NC allow it with attribution
# (which data/audio/manifest.csv provides), so committing is fine and makes the
# deployed app self-contained. Delete the two lines below if you would rather
# keep the repo slim and have each user run `python src/fetch_audio.py`.
# data/audio/*.mp3
# data/audio/manifest.csv

# NOTE: data/ is deliberately NOT ignored.
# The Streamlit Cloud deployment reads data/processed/*.csv directly, so those
# files have to be in the repo for the hosted app to start. They total about
# 13 MB, which is well inside GitHub's limits. data/raw/*.xlsx (2 MB) is kept
# too so the whole pipeline can be re-run from source by anyone who clones it.
#
# docs/*.pdf and docs/*.docx are also committed on purpose: they are the
# deliverable, and committing them means a reader can download the report
# without installing Python or running the generator.