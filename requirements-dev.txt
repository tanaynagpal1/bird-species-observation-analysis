# Local development environment.
# Installs everything in requirements.txt plus notebook tooling.
#   pip install -r requirements-dev.txt
-r requirements.txt
jupyter==1.1.1

# Only needed to run src/compress_audio.py once, to shrink the ear-test
# recordings. Ships prebuilt libsndfile wheels, so no ffmpeg or system install.
# The dashboard itself never imports it - it is not in requirements.txt.
soundfile>=0.12